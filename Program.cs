// Сортировка выбором
using static Sorting;
using static Infrastructure;
// int[] basicArray = CreateArray(10);
// Show(basicArray);
// SortSelection(basicArray);
// Show(basicArray);

10.CreateArray()
  .Show("|")
  .SortSelection()
  .Show("><");

